import 'package:flutter/material.dart';

class MaintenanceScreenA extends StatelessWidget {
  const MaintenanceScreenA({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("MAINTENANCE - TO BE UPDATED"),
      ),
    );
  }
}